{
  "name": "@helix-heal/cli",
  "version": "0.1.0",
  "description": "Helix Heal command line interface.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "helix-heal": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist",
    "helix-heal": "node dist/index.js",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@helix-heal/core": "0.1.0",
    "@helix-heal/playwright-ingest": "0.1.0"
  },
  "devDependencies": {
    "rimraf": "^5.0.5"
  }
}
